version: "3.3"

networks:
  lnnet:

volumes:
  alice-data:

services:
  bitcoind:
    container_name: bitcoind
    image: bitcoind:24.0.1
    networks:
      - lnnet
    expose:
      - "18443"
      - "12005"
      - "12006"

  Alice:
    container_name: Alice
    image: lnd:0.15.5-beta
    networks:
      - lnnet
    expose:
      - "9735"
      - "8080"
      - "10009"
    volumes:
      - alice-data:/lnbits:rw
    environment:
      - tlsextradomain=Alice
      - suportlnbits=1

  lnbits:
    container_name: lnbits
    image: lnbits:latest
    networks:
      - lnnet
    ports:
      - 5000:5000
    volumes:
      - alice-data:/lnd:ro
    environment:
      - LND_GRPC_ENDPOINT=Alice

  Bob:
    container_name: Bob
    image: lnd:0.15.5-beta
    networks:
      - lnnet
    expose:
      - "9735"
      - "10009"
    environment:
      - tlsextradomain=Bob
      - suportlnbits=0
